type Type_PropDrill = {
  title: string;
  backdrop: string;
  overview: string;
};

const FDVideoPlayerPanel = ({ title, backdrop, overview }: Type_PropDrill) => {
  return <section className='FDVideoPlayer__panel'></section>;
};
export default FDVideoPlayerPanel;
